# coffee-dam

## Development checks

This repo uses `ruff` for Python linting/formatting and `pre-commit` for local checks.

Install the tools and enable the hooks:

```bash
pip install pre-commit ruff
pre-commit install
pre-commit run --all-files
```
